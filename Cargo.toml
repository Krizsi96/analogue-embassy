# This file was automatically generated.

[package]
edition = "2021"
name = "analogue-embassy"
version = "0.1.0"

[dependencies]
cortex-m = { version = "0.7.7", optional = true, features = ["inline-asm", "critical-section-single-core"] }
cortex-m-rt = { version = "0.7.5", optional = true }
defmt = { version = "1.0.1", optional = true }
defmt-rtt = { version = "1.0.0", optional = true }
embassy-executor = { version = "0.9.1", optional = true, features = ["arch-cortex-m", "executor-thread"] }
embassy-stm32 = { version = "0.4.0", optional = true, features = ["defmt", "stm32f407vg", "unstable-pac", "memory-x", "time-driver-any", "exti"] }
embassy-futures = { version = "0.1.2", optional = true }
embassy-sync = { version = "0.7.2", optional = true }
embassy-time = { version = "0.5.0", optional = true, features = ["tick-hz-32_768"] }
panic-halt = { version = "1.0.0", optional = true }
panic-probe = { version = "1.0.0", features = ["print-defmt"], optional = true }

[[bin]]
name = "analogue-embassy"
test = false
bench = false

[profile.dev]
debug = true
lto = true
opt-level = "z"
incremental = true

[profile.release]
debug = false
lto = true
opt-level = "z"
incremental = true

[features]
defmt = ["dep:defmt"]
defmt-rtt = ["dep:defmt-rtt"]
panic-probe = ["dep:panic-probe"]
default = ["debug"]
debug = [
    "defmt",
    "defmt-rtt",
    "panic-probe",
    "embassy-executor/defmt",
    "embassy-sync/defmt",
    "embassy-futures/defmt",
    "embassy-time/defmt",
    "embassy-time/defmt-timestamp-uptime",
    "embassy-stm32/defmt",
]

embedded = [
    "cortex-m",
    "cortex-m-rt",
    "embassy-executor",
    "embassy-stm32",
    "embassy-sync",
    "embassy-time",
    "embassy-futures",
]

[dev-dependencies]
rstest = "0.26.1"
